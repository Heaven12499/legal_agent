# ========== 法律 RAG 依赖清单 ==========
# 在项目根目录安装：python -m pip install -r backend/requirements.txt

# ---- 向量化与精排（本地 bge 系列，无需 API key，可复现）----
# embedding: BAAI/bge-small-zh-v1.5 ；rerank（P1）: BAAI/bge-reranker-base
sentence-transformers==6.0.0
# 模型下载脚本直接 import 的（间接依赖也显式钉死，保证可复现）
huggingface_hub==1.28.0
numpy==2.5.2

# ---- 向量检索库 ----
faiss-cpu==1.15.0

# ---- 词法检索（BM25 短锚句词面匹配）----
rank-bm25==0.2.2
jieba==0.42.1

# ---- Agent loop（DeepSeek OpenAI 兼容接口，手写 while 循环）----
openai==3.3.1

# ---- Web 服务（M4：FastAPI 后端 + uvicorn）----
fastapi==0.141.1
uvicorn==0.52.4
python-multipart==0.0.20

# ---- 合同文件上传解析（M5：docx/pdf/txt -> 纯文本，复用现有 agent）----
python-docx==1.2.0
pypdf==6.16.2

# ---- 用户认证（M9：argon2 密码哈希 + JWT 签发/校验）----
argon2-cffi==25.1.0
PyJWT==2.13.0
